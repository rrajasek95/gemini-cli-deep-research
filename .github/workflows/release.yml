name: Release Extension

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build extension
        run: npm run build

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Update versions in config files
        run: |
          # Update package.json version
          npm version ${{ steps.version.outputs.VERSION }} --no-git-tag-version
          # Update gemini-extension.json version
          jq '.version = "${{ steps.version.outputs.VERSION }}"' gemini-extension.json > tmp.json && mv tmp.json gemini-extension.json

      - name: Create release config
        run: |
          # Create release-specific gemini-extension.json that runs node directly (no npm required)
          jq '.mcpServers["gemini-deep-research"].command = "node" | .mcpServers["gemini-deep-research"].args = ["dist/index.js"]' gemini-extension.json > release-gemini-extension.json

      - name: Create release archive
        run: |
          mkdir -p release
          # Create archive with only the necessary runtime files
          tar -czvf release/gemini-deep-research.tar.gz \
            --transform 's|release-gemini-extension.json|gemini-extension.json|' \
            dist \
            commands \
            node_modules \
            release-gemini-extension.json \
            deep-research-GEMINI.md

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/gemini-deep-research.tar.gz
          generate_release_notes: true
          make_latest: true
